defaults:
  - dataset: halfcheetah_medium
  - world_model: mle
  - policy_optimizer: mopo
  - networks: mlp
  - _self_

seed: 0
stage: all
checkpoint_dir: checkpoints

wandb:
  enabled: false
  entity: model-based-eggroll
  group: null   # auto-generated if null: {world_model}-{policy_optimizer}-{dataset}
  name: null    # auto-generated if null: {group}-{stage_suffix}-s{seed}

eval:
  num_episodes: 10
