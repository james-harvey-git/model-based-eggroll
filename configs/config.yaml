defaults:
  - dataset: halfcheetah_medium
  - world_model: mle
  - algorithm: mopo
  - networks: mlp
  - _self_

seed: 0
stage: all
checkpoint_dir: checkpoints

wandb:
  enabled: false
  entity: model-based-eggroll

eval:
  num_episodes: 10
